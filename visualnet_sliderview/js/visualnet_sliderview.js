
(function ($, Drupal, drupalSettings) {

    $('#article').carouseller({
        easing: 'linear'
    });

    $('#news').carouseller({
        easing: 'linear'
    });

    $('#guest').carouseller({
        easing: 'linear'
    });

})(jQuery, Drupal, drupalSettings);